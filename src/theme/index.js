

import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1eb955',
      contrastText: '#FFFFFF',
    },
    secondary: {
      light: '#282828',
      main: '#181818',
    },
    type: 'dark',
  },
});
